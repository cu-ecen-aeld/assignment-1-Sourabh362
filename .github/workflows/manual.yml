name: CI Workflow

on:
  workflow_dispatch:  # Allows manual triggering

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a script
        run: echo "Hello, World!"
